import { FormatPostDateTimePipe } from './format-post-date-time.pipe';

describe('FormatPostDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPostDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
