import { DragNDropDirective } from './index';

describe('DragndropDirective', () => {
  it('should create an instance', () => {
    const directive = new DragNDropDirective();
    expect(directive).toBeTruthy();
  });
});
